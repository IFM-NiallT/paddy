<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PADDY Categories</title>
   
    <!-- External Libraries -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Liter:wght@400;700&display=swap" rel="stylesheet">
   
    <!-- Our Custom Stylesheet -->
    <link rel="stylesheet" href="{{ url_for('static', filename='css/paddy.css') }}">
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid px-4">
            <a class="navbar-brand" href="#">PADDY</a>
            <div>
                <button id="searchAllProducts" class="btn-uni">
                    Search All Products
                </button>
            </div>
        </div>
    </nav>

    <!-- Search Bar Container -->
    <div class="search-bar-container">
        <div class="search-controls">
            <!-- Category Search - Shown in category view -->
            <div class="search-wrapper" id="categorySearchWrapper">
                <input type="text" class="search-input" id="categorySearch" placeholder="Search categories...">
            </div>
            
            <!-- Product Search - Shown in product view -->
            <div class="search-wrapper" id="productSearchWrapper" style="display: none;">
                <input type="text" class="search-input" id="productSearch" placeholder="Search products by code...">
            </div>
        </div>
    </div>

    <!-- Rest of the code remains the same as in the original file -->
    <main class="container-fluid px-4">
        <!-- Categories View -->
        <div id="category-grid" class="row row-cols-1 row-cols-sm-2 row-cols-md-4 row-cols-lg-6 row-cols-xxl-8 g-4">
            {% for category in categories.Data %}
                <div class="col category-item">
                    <div class="category-container">
                        <div class="category-content">
                            <div class="category-name">{{ category.Description }}</div>
                            <div class="category-button">
                                <button class="btn-uni" onclick="window.location.href='/products/{{ category.ID }}'">
                                    View Products
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>

        <!-- Products View -->
        <div id="productSearchResults" style="display: none;">
            <div class="view-header">
                <h2 class="view-title">All Products</h2>
                <button class="back-button" onclick="toggleSearchMode(false)">
                    ← Back to Categories
                </button>
            </div>
            
            <div class="table-container">
            <div class="table-responsive">
                <table class="table table-bordered" id="productsTable">
                    <thead>
                        <tr>
                            <th>Code</th>
                            <th>Description</th>
                            <th>Category</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody id="productResultsBody">
                        <!-- Results will be populated here -->
                    </tbody>
                </table>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer>
        <p>PADDY &copy; 2025 - IFM</p>
        <p class="quote">"Product Attribute Designer Deployment (for) You"</p>
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ url_for('static', filename='js/categories.js') }}"></script>
</body>
</html>