<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PADDY - Error</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=VT323&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: #0d0d0d;
            color: #d6d6d6;
            font-family: 'VT323', monospace;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        nav.navbar {
            background: linear-gradient(90deg, #ff8c00, #00ff8c);
            border-bottom: 3px solid #101010;
        }

        nav a.navbar-brand {
            color: #101010;
            font-family: 'Orbitron', sans-serif;
            text-shadow: 0 0 5px #00ff8c, 0 0 10px #ff8c00;
        }

        .error-container {
            text-align: center;
            margin-top: 5rem;
        }

        .error-heading {
            color: #ff8c00;
            font-family: 'Orbitron', sans-serif;
            text-shadow: 0 0 5px #ff8c00;
        }

        .error-message {
            color: #00ff8c;
            font-size: 1.5rem;
            margin: 2rem 0;
        }

        .btn-uni {
            background-color: #101010;
            color: #ff8c00;
            border: 2px solid #00ff8c;
            font-family: 'VT323', monospace;
            font-size: 1rem;
            padding: 0.5rem 2rem;
            transition: all 0.3s ease;
        }

        .btn-uni:hover {
            background-color: #ff8c00;
            color: #000;
        }

        footer {
            background-color: #101010;
            color: #d6d6d6;
            padding: 10px;
            text-align: center;
            width: 100%;
            margin-top: auto;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="/">PADDY</a>
        </div>
    </nav>

    <main>
        <div class="container error-container">
            <h1 class="error-heading">System Error</h1>
            <p class="error-message">{{ error }}</p>
            <button class="btn-uni" onclick="window.location.href='/'">Return to Home</button>
        </div>
    </main>

    <footer>
        <p>PADDY &copy; 2025 - Product Attribute Designer Deployment for You</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>