<!DOCTYPE html>
<html lang="en" class="no-js">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PADDY - Error</title>

    <!-- Critical Meta Tags -->
    <meta name="description" content="PADDY: Product Attribute Designer Deployment error page">
    <meta name="author" content="IFM">

    <!-- External Libraries -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Liter:wght@400;700&display=swap" rel="stylesheet">

    <!-- Main Stylesheet -->
    <link rel="stylesheet" href="{{ url_for('static', filename='css/main.css') }}">
</head>

<body>
    <!-- Navigation Bar -->
    <header>
        <nav class="navbar" aria-label="Main Navigation">
            <div class="container container--fluid">
                <div class="navbar__brand">
                    <img
                        src="{{ url_for('static', filename='img/ifm_logo.png') }}"
                        alt="IFM logo"
                        class="navbar__logo"
                        loading="lazy"
                    >
                    <a
                        class="navbar__title text-decoration-none"
                        href="/"
                        aria-label="PADDY Home"
                    >
                        PADDY
                    </a>
                </div>
            </div>
        </nav>
    </header>

    <!-- Main Content -->
    <main class="main">
        <div class="container container--fluid">
            <div class="error-container">
                <div class="error-content">
                    <h1 class="error-title">System Error</h1>
                    <p class="error-message">{{ error }}</p>
                    <div class="error-actions">
                        <button 
                            class="btn-uni" 
                            onclick="window.location.href='/'"
                            aria-label="Return to Homepage"
                        >
                            Return to Home
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer" role="contentinfo">
        <p class="footer__copyright">PADDY &copy; 2025 - IFM</p>
        <p class="footer__quote">"Product Attribute Designer Deployment (for) You"</p>
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>